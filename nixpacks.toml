# Force the Node/npm toolchain (the repo's bun.lockb otherwise makes Nixpacks
# pick Bun, which leaves npm unavailable for `npm run build`).
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
